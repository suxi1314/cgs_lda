#如果是有向图
## 第0第0个superstep或者load_graph的时候赋topic初值给edge
## odd superstep
if vertex is_doc
    就gather edge上的topic, add to vector
    send edge上的topic给word
## even superstep
if vertex is_word
    recv edge topic , add to vector
    send vertex data to word？不可行
有向图不可行

#如果是有向的无向图
读取有向的无向图，需要在数据集(test_graph_4w)中添加doc和word的个数，

并在load_graph的时候读出来， 利用doc和word的个数判断是何种节点，并标记flag
